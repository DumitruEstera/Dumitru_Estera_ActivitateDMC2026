<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Dumitru_Estera_ActivitateDMC2026">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_api_key" />

        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.DashboardActivity"
            android:exported="false" />

        <activity
            android:name=".activities.AlarmsListActivity"
            android:exported="false"
            android:label="@string/alarms_title"
            android:parentActivityName=".activities.DashboardActivity" />

        <activity
            android:name=".activities.AlarmDetailActivity"
            android:exported="false"
            android:parentActivityName=".activities.AlarmsListActivity" />

        <activity
            android:name=".activities.PersonsListActivity"
            android:exported="false"
            android:label="@string/persons_title"
            android:parentActivityName=".activities.DashboardActivity" />

        <activity
            android:name=".activities.PersonDetailActivity"
            android:exported="false"
            android:parentActivityName=".activities.PersonsListActivity" />

        <activity
            android:name=".activities.CameraMapActivity"
            android:exported="false"
            android:label="@string/map_title"
            android:parentActivityName=".activities.DashboardActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>
